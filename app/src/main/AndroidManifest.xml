<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="breakthecode.com.clickandgo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.MyTicketsActivity"></activity>
        <activity android:name=".activities.TicketCardActivity" />
        <activity android:name=".activities.TicketBuyingActivity" />
        <activity android:name=".activities.RideCardActivity" />
        <activity android:name=".activities.ChooseRideActivity" />
        <activity android:name=".activities.ChooseCityToActivity" />
        <activity android:name=".activities.ChooseCityFromActivity" />
        <activity android:name=".activities.MainPanelActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>